import React from 'react'
import AuthTemplate from '../../components/auth/AuthTemplate';

export default function Register() {
    return (
        <AuthTemplate subTitle={"Explore the best courses online with thousands of classes in networking, cybersecurity, os & IT, and many more."} title={"Sign up to CyberShield"} isRegister={true} />
    )
}